import { NesTextDirective } from './nes-text.directive';

describe('NesTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NesTextDirective();
    expect(directive).toBeTruthy();
  });
});
